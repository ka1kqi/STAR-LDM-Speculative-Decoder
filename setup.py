from setuptools import setup, find_packages

setup(
    name="diff_spec_ar_lm",
    version="0.1.0",
    description="Hybrid Diffusion–Speculative Autoregressive LM (STAR-LDM variant)",
    packages=find_packages(),
    python_requires=">=3.10",
)
